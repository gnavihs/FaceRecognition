# FaceRecognition

session.py -> initializes and runs the session.  
DataImport.py -> Downloads data, convert them to numPy arrays, splits into train and test set.  
graph.py -> Defines the graph to be used by tensorFlow including Convo layer, pooling layer, FC layer, cost, optimizer, accuracy.  
model.py -> Defines the functions used in graph.  
